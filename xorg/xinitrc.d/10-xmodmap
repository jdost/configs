if which xmodmap &>/dev/null; then
  [[ -e "${XDG_CONFIG_HOME:-$HOME/.config}/xorg/caps-ctrl-swap.xmodmap" ]] && \
    xmodmap "${XDG_CONFIG_HOME:-$HOME/.config}/xorg/caps-ctrl-swap.xmodmap"
fi

# vim: ft=bash
